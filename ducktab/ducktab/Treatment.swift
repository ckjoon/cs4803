//
//  Treatment.swift
//  ducktab
//
//  Created by Nishant Panchal on 12/2/16.
//  Copyright © 2016 gatech.edu. All rights reserved.
//

import Foundation
import UIKit

class Treatment: UIViewController {

    override func prepareForSegue(segue: UIStoryboardSegue, sender: AnyObject?) {
        if (segue.identifier == "noToChar"){
              //Send Data to Firebase
        }
        if (segue.identifier == "yesToChar"){
            //Send Data to Firebase
        }
        if (segue.identifier == "hemToChar"){
            //Send Data to Firebase
        }
        if (segue.identifier == "oncoToChar"){
            //Send Data to Firebase
        }
        if (segue.identifier == "noSureToChar"){
            //Send Data to Firebase
        }
    }
}

